# UI

The Flutter shell can prepare bridge payloads and later send them to the Rust engine through the bridge contract.

The local bridge client currently encodes and decodes JSON payloads as a frontend-side integration helper.
