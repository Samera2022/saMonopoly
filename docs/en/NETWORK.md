# Network

The infrastructure layer includes a disabled network transport scaffold for future multiplayer or session sync support.

The current contract is message-based and uses an endpoint plus serialized payloads.
