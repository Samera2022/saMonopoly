# Content Loading

The Flutter shell includes a JSON content pack loader that can map source data into board and tile view models.

The app also carries a sample asset pack and can load a hard-coded JSON example for prototyping.
