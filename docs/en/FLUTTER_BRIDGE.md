# Flutter Bridge Client

The Flutter shell includes a lightweight bridge client that encodes outbound requests and decodes inbound responses.

This client is ready to be replaced with a native transport once Rust FFI or WebAssembly plumbing is connected.
