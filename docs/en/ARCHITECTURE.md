# Architecture

See the root `ARCHITECTURE.md` for the current system design.

The current foundation uses a Rust shared core with Flutter shells and explicit bridge, persistence, content, plugin, scripting, and network boundaries.
