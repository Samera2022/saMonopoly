# Board Rendering

The Flutter shell now holds a small board view model that can later be populated from content packs and rendered as tiles.

A sample content pack view model is wired into the home screen for UI prototyping.

The UI now displays the content pack id, version, board name, and tile chips.
